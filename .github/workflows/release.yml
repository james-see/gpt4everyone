name: Build and Release GCP4ALL

on:
  push:
    tags:
      - 'v*'

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.8.0'
          host: 'mac'
          target: 'desktop'
          arch: 'clang_64'
          modules: 'qt5compat qthttpserver qtpdf qtwebsockets'

      - name: Install dependencies
        run: |
          brew install ninja cmake
          python3 -m pip install click
          # Patch usearch dependency which has a typo breaking compilation on newer Clang
          sed -i '' '3378s/index_->node_at_/index_.node_at_/' gpt4all-chat/deps/usearch/include/usearch/index.hpp

      - name: Import Code Signing Certificate
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.MACOS_CERTIFICATE }}
          p12-password: ${{ secrets.MACOS_CERTIFICATE_PWD }}

      - name: Configure CMake
        run: |
          cd gpt4all-chat
          mkdir build
          cd build
          cmake .. -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DGPT4ALL_TEST=OFF \
            -DGPT4ALL_SIGN_INSTALL=ON \
            -DMAC_SIGNING_IDENTITY="${{ secrets.MAC_SIGNING_IDENTITY }}" \
            -DMAC_SIGNING_TID="${{ secrets.APPLE_TEAM_ID }}"

      - name: Build
        run: |
          cd gpt4all-chat/build
          ninja

      - name: Deploy Qt Dependencies
        run: |
          # Find macdeployqt in the Qt installation directory
          QT_PATH=$(find /Users/runner/work/gpt4everyone/Qt -maxdepth 2 -name "6.*" -type d | head -n 1)
          MACDEPLOYQT="${QT_PATH}/macos/bin/macdeployqt"
          echo "Using macdeployqt at ${MACDEPLOYQT}"
          
          ${MACDEPLOYQT} gpt4all-chat/build/bin/gpt4all.app \
            -qmldir=gpt4all-chat \
            -verbose=2

      - name: Sign App Bundle
        run: |
          # Sign all dylibs in Frameworks individually with hardened runtime and timestamp
          # This is required for notarization
          if [ -d "gpt4all-chat/build/bin/gpt4all.app/Contents/Frameworks" ]; then
            find gpt4all-chat/build/bin/gpt4all.app/Contents/Frameworks -name "*.dylib" -exec codesign --force --options runtime --timestamp --sign "${{ secrets.MAC_SIGNING_IDENTITY }}" {} \;
          fi
          
          # Sign all plugins if they exist
          if [ -d "gpt4all-chat/build/bin/gpt4all.app/Contents/PlugIns" ]; then
            find gpt4all-chat/build/bin/gpt4all.app/Contents/PlugIns -name "*.dylib" -exec codesign --force --options runtime --timestamp --sign "${{ secrets.MAC_SIGNING_IDENTITY }}" {} \;
          fi
          
          # Sign the main executable
          codesign --force --options runtime --timestamp --sign "${{ secrets.MAC_SIGNING_IDENTITY }}" gpt4all-chat/build/bin/gpt4all.app/Contents/MacOS/gpt4all
          
          # Sign the app bundle itself
          codesign --force --options runtime --timestamp --sign "${{ secrets.MAC_SIGNING_IDENTITY }}" gpt4all-chat/build/bin/gpt4all.app

      - name: Package DMG
        run: |
          # Create a DMG manually using hdiutil
          hdiutil create -volname "GCP4ALL" -srcfolder gpt4all-chat/build/bin/gpt4all.app -ov -format UDZO gpt4all-chat/build/gpt4all-installer-darwin.dmg

      - name: Sign DMG
        run: |
          codesign --force --timestamp --sign "${{ secrets.MAC_SIGNING_IDENTITY }}" gpt4all-chat/build/gpt4all-installer-darwin.dmg

      - name: Notarize DMG
        run: |
          # Submit for notarization and wait
          xcrun notarytool submit gpt4all-chat/build/gpt4all-installer-darwin.dmg \
            --apple-id "${{ secrets.APPLE_ID }}" \
            --password "${{ secrets.APPLE_PASSWORD }}" \
            --team-id "${{ secrets.APPLE_TEAM_ID }}" \
            --wait > notarization_output.txt 2>&1
          
          cat notarization_output.txt
          
          SUBMISSION_ID=$(grep "id:" notarization_output.txt | head -n 1 | awk '{print $2}')
          
          if ! grep -q "status: Accepted" notarization_output.txt; then
            echo "Notarization failed!"
            if [ -n "$SUBMISSION_ID" ]; then
              xcrun notarytool log $SUBMISSION_ID \
                --apple-id "${{ secrets.APPLE_ID }}" \
                --password "${{ secrets.APPLE_PASSWORD }}" \
                --team-id "${{ secrets.APPLE_TEAM_ID }}"
            fi
            exit 1
          fi

      - name: Staple DMG
        run: |
          xcrun stapler staple gpt4all-chat/build/gpt4all-installer-darwin.dmg

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          files: gpt4all-chat/build/*.dmg
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
